
import 'package:flutter/material.dart';
// Ensure that the file appBar_model.dart defines a class named AppBarModel.
// import '/flutter_flow/flutter_flow_theme.dart'; // Removed because the file does not exist

class HomeModel {
  ///  State fields for stateful widgets in this page.

  late dynamic drawerModel;
  late dynamic saiderBarModel;
  late dynamic appBarMobileModel;
  late dynamic appBarModel;

  void initState(BuildContext context) {
    drawerModel = Object();
    saiderBarModel = Object();
    appBarMobileModel = Object();
    appBarModel = Object();
  }

  void dispose() {
    // Add dispose logic if your models have dispose methods
  }
}
